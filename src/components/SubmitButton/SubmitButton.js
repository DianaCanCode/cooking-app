import React from "react";
import "./SubmitButton.css";

//Search bar: 
const SearchBar = props => {
  return (
    <div className='container'> 
         <input className="button" type="submit" value="Preview List"></input>
    </div>
  );
};

export default SearchBar;
